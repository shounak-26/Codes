num = int(input('Enter number: '))
arr = []

for i in range (2,num):
    flag = 0
    for j in range (2,i):
        if i % j == 0:
            flag = 1

    if flag == 0 :
        arr.append(i)
print(arr)
